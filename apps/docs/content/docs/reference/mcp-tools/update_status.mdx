---
title: "update_status"
description: "Rename / recolor / recategorize a status."
---

{/* Generated by apps/docs/scripts/generate-mcp-pages.mjs — do not edit by hand. */}

Rename / recolor / recategorize a status.

| | |
| --- | --- |
| Required permission | `work:write` |
| Capability ID | `statuses.update` |
| Destructive | No |

## Parameters

| Parameter | Type | Required | Notes |
| --- | --- | --- | --- |
| `name` | string | no |  |
| `category` | `BACKLOG` \| `UNSTARTED` \| `STARTED` \| `COMPLETED` \| `CANCELLED` | no |  |
| `color` | string | no |  |
| `id` | string (uuid) | yes |  |

## Input schema (JSON Schema)

```json
{
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 60
    },
    "category": {
      "type": "string",
      "enum": [
        "BACKLOG",
        "UNSTARTED",
        "STARTED",
        "COMPLETED",
        "CANCELLED"
      ]
    },
    "color": {
      "type": "string",
      "minLength": 1,
      "maxLength": 32
    },
    "id": {
      "type": "string",
      "format": "uuid"
    }
  },
  "required": [
    "id"
  ],
  "additionalProperties": false
}
```

## Access control

A call succeeds only when the personal access token's scopes **and** the holder's role both allow `work:write` (effective permission = scope ∩ role, default-deny). The tool runs inside the token owner's organization — tenancy is never a parameter.

---
title: "list_views"
description: "List saved views visible to the principal (own personal + shared in projects) — a RyTask MCP tool for AI agents over the Model Context Protocol (requires the work:read permission)."
---

{/* Generated by apps/docs/scripts/generate-mcp-pages.mjs — do not edit by hand. */}

List saved views visible to the principal (own personal + shared in projects).

| | |
| --- | --- |
| Required permission | `work:read` |
| Capability ID | `views.list` |
| Destructive | No |

## Parameters

| Parameter | Type | Required | Notes |
| --- | --- | --- | --- |
| `projectId` | string (uuid) | no |  |

## Input schema (JSON Schema)

```json
{
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "format": "uuid"
    }
  },
  "additionalProperties": false
}
```

## Access control

A call succeeds only when the personal access token's scopes **and** the holder's role both allow `work:read` (effective permission = scope ∩ role, default-deny). The tool runs inside the token owner's organization — tenancy is never a parameter.

---
title: "remove_label_from_issue"
description: "Remove a label from a work item — a RyTask MCP tool for AI agents over the Model Context Protocol (requires the work:write permission)."
---

{/* Generated by apps/docs/scripts/generate-mcp-pages.mjs — do not edit by hand. */}

Remove a label from a work item.

| | |
| --- | --- |
| Required permission | `work:write` |
| Capability ID | `workItems.removeLabel` |
| Destructive | No |

## Parameters

| Parameter | Type | Required | Notes |
| --- | --- | --- | --- |
| `id` | string (uuid) | yes |  |
| `labelId` | string (uuid) | yes |  |

## Input schema (JSON Schema)

```json
{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid"
    },
    "labelId": {
      "type": "string",
      "format": "uuid"
    }
  },
  "required": [
    "id",
    "labelId"
  ],
  "additionalProperties": false
}
```

## Access control

A call succeeds only when the personal access token's scopes **and** the holder's role both allow `work:write` (effective permission = scope ∩ role, default-deny). The tool runs inside the token owner's organization — tenancy is never a parameter.

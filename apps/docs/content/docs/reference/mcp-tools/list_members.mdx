---
title: "list_members"
description: "List members of the organization and their roles — a RyTask MCP tool for AI agents over the Model Context Protocol (requires the members:read permission)."
---

{/* Generated by apps/docs/scripts/generate-mcp-pages.mjs — do not edit by hand. */}

List members of the organization and their roles.

| | |
| --- | --- |
| Required permission | `members:read` |
| Capability ID | `members.list` |
| Destructive | No |

## Parameters

This tool takes no parameters — call it with an empty object.

## Input schema (JSON Schema)

```json
{
  "type": "object",
  "properties": {},
  "additionalProperties": false
}
```

## Access control

A call succeeds only when the personal access token's scopes **and** the holder's role both allow `members:read` (effective permission = scope ∩ role, default-deny). The tool runs inside the token owner's organization — tenancy is never a parameter.

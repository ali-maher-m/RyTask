---
title: "list_projects"
description: "List accessible projects in the workspace (keyset paginated)."
---

{/* Generated by apps/docs/scripts/generate-mcp-pages.mjs — do not edit by hand. */}

List accessible projects in the workspace (keyset paginated).

| | |
| --- | --- |
| Required permission | `work:read` |
| Capability ID | `projects.list` |
| Destructive | No |

## Parameters

| Parameter | Type | Required | Notes |
| --- | --- | --- | --- |
| `filter` | string | no |  |
| `limit` | integer | no |  |
| `cursor` | string | no |  |
| `fields` | array of string | no |  |

## Input schema (JSON Schema)

```json
{
  "type": "object",
  "properties": {
    "filter": {
      "type": "string"
    },
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 100,
      "default": 50
    },
    "cursor": {
      "type": "string"
    },
    "fields": {
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  },
  "additionalProperties": false
}
```

## Access control

A call succeeds only when the personal access token's scopes **and** the holder's role both allow `work:read` (effective permission = scope ∩ role, default-deny). The tool runs inside the token owner's organization — tenancy is never a parameter.

---
title: "reorder_statuses"
description: "Reorder a project’s statuses (board column order)."
---

{/* Generated by apps/docs/scripts/generate-mcp-pages.mjs — do not edit by hand. */}

Reorder a project’s statuses (board column order).

| | |
| --- | --- |
| Required permission | `work:write` |
| Capability ID | `statuses.reorder` |
| Destructive | No |

## Parameters

| Parameter | Type | Required | Notes |
| --- | --- | --- | --- |
| `orderedIds` | array of string (uuid) | yes |  |
| `projectId` | string (uuid) | yes |  |

## Input schema (JSON Schema)

```json
{
  "type": "object",
  "properties": {
    "orderedIds": {
      "type": "array",
      "items": {
        "type": "string",
        "format": "uuid"
      }
    },
    "projectId": {
      "type": "string",
      "format": "uuid"
    }
  },
  "required": [
    "orderedIds",
    "projectId"
  ],
  "additionalProperties": false
}
```

## Access control

A call succeeds only when the personal access token's scopes **and** the holder's role both allow `work:write` (effective permission = scope ∩ role, default-deny). The tool runs inside the token owner's organization — tenancy is never a parameter.

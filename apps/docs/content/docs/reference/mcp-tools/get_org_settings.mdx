---
title: "get_org_settings"
description: "Read the organization settings (timezone, locale, week start, working hours…) — a RyTask MCP tool for AI agents over the Model Context Protocol (requires the org:read permission)."
---

{/* Generated by apps/docs/scripts/generate-mcp-pages.mjs — do not edit by hand. */}

Read the organization settings (timezone, locale, week start, working hours…).

| | |
| --- | --- |
| Required permission | `org:read` |
| Capability ID | `orgs.settings.get` |
| Destructive | No |

## Parameters

This tool takes no parameters — call it with an empty object.

## Input schema (JSON Schema)

```json
{
  "type": "object",
  "properties": {},
  "additionalProperties": false
}
```

## Access control

A call succeeds only when the personal access token's scopes **and** the holder's role both allow `org:read` (effective permission = scope ∩ role, default-deny). The tool runs inside the token owner's organization — tenancy is never a parameter.

# Claude Code local config / installed skills — not for the public repo,
# except the update-docs skill, which is committed so docs stay in sync with code
.claude/*
!.claude/skills/
.claude/skills/*
!.claude/skills/update-docs/

# Internal working artifacts — not for the public repo
goals/
branding/chats/
branding/HANDOFF.md

# Local-only compose override (holds your local JWT_SECRET — never commit)
docker-compose.override.yml

# macOS
.DS_Store

# Dependencies
node_modules/

# Build output
dist/
.next/
out/
*.tsbuildinfo

# Turborepo
.turbo/

# Test / coverage
coverage/
playwright-report/
test-results/

# Environment (only .env.example is committed)
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Claude Code personal settings
.claude/settings.local.json
